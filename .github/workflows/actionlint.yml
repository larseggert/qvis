name: Lint workflows

on:
  pull_request:

permissions:
  contents: read

jobs:
  actionlint:
    uses: mozilla/actions/.github/workflows/actionlint.yml@32d84c77ea1bb513afbd253f94f67ae138b7d8be # v2.0.10
    permissions:
      contents: read
      security-events: write # Required for zizmor to upload SARIF results
